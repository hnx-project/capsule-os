[package]
name = "libc"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["rlib"]

[dependencies]
libcapsule = { path = "../libcapsule" }
shared = { path = "../../kernel/shared" }
