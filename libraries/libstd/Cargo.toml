[package]
name = "libstd"
version.workspace = true
edition.workspace = true

[dependencies]
libc = { path = "../libc" }
libcapsule = { path = "../libcapsule" }
shared = { path = "../../kernel/shared" }

[features]
default = []
