[package]
name = "hal"
version = "0.1.0"
edition = "2021"
description = "Hardware Abstraction Layer for Capsule OS"

[features]
default = []

[dependencies]
shared = { path = "../shared" }

[lints.rust]
unsafe_code = "forbid"
