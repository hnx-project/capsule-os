[package]
name = "hello_pill"
version.workspace = true
edition.workspace = true

[[bin]]
name = "hello_pill"
path = "src/main.rs"

[dependencies]
libpillsmod = { workspace = true, features = ["panic-handler"] }
