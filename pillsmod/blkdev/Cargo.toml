[package]
name = "hnx-blkdev"
version.workspace = true
edition.workspace = true

[[bin]]
name = "blkdev"
path = "src/main.rs"

[dependencies]
modskit = { path = "../../libraries/modskit", default-features = false }
shared = { path = "../../kernel/shared" }

[features]
default = []
capsule = []