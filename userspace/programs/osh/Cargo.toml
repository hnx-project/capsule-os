[package]
name = "hnx-osh"
version.workspace = true
edition.workspace = true

[features]
default = ["host"]
host = []
capsule = ["dep:libstd"]

[[bin]]
name = "osh"
path = "src/main.rs"

[dependencies]
libc = { workspace = true }
libstd = { workspace = true, optional = true }
libcapsule = { workspace = true }
