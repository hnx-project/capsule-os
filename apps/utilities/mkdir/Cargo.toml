[package]
name = "hnx-mkdir"
version.workspace = true
edition.workspace = true

[features]
default = ["host"]
host = []
capsule = []

[[bin]]
name = "mkdir"
path = "src/main.rs"

[dependencies]
libstd = { workspace = true }
